# One-row model summary for an `lmm` model

One-row model summary for an `lmm` model

## Usage

``` r
# S3 method for class 'lmm'
glance(x, ...)
```

## Arguments

- x:

  An `lmm` object.

- ...:

  Additional arguments.

## Value

A one-row tibble.
