% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{lmm-methods}
\alias{lmm-methods}
\alias{print.lmm}
\alias{coef.lmm}
\alias{vcov.lmm}
\alias{logLik.lmm}
\alias{AIC.lmm}
\alias{BIC.lmm}
\alias{deviance.lmm}
\alias{nobs.lmm}
\alias{fitted.lmm}
\alias{residuals.lmm}
\alias{plot.lmm}
\alias{sigma.lmm}
\alias{predict.lmm}
\alias{simulate.lmm}
\alias{update.lmm}
\alias{model.matrix.lmm}
\alias{model.frame.lmm}
\alias{formula.lmm}
\alias{terms.lmm}
\alias{anova.lmm}
\alias{confint.lmm}
\title{Standard methods for \code{lmm} fits}
\usage{
\method{print}{lmm}(x, ...)

\method{coef}{lmm}(object, ...)

\method{vcov}{lmm}(object, adjusted = TRUE, ...)

\method{logLik}{lmm}(object, ...)

\method{AIC}{lmm}(object, ..., k = 2)

\method{BIC}{lmm}(object, ...)

\method{deviance}{lmm}(object, ...)

\method{nobs}{lmm}(object, ...)

\method{fitted}{lmm}(object, type = c("conditional", "marginal"), ...)

\method{residuals}{lmm}(object, type = c("response", "pearson", "marginal"), ...)

\method{plot}{lmm}(x, which = c("residuals", "qq", "fitted"), ...)

\method{sigma}{lmm}(object, ...)

\method{predict}{lmm}(
  object,
  newdata = NULL,
  re.form = NULL,
  allow.new.levels = FALSE,
  se.fit = FALSE,
  interval = c("none", "confidence", "prediction"),
  level = 0.95,
  na.action = stats::na.pass,
  ...
)

\method{simulate}{lmm}(object, nsim = 1, seed = NULL, ...)

\method{update}{lmm}(object, formula., ..., evaluate = TRUE)

\method{model.matrix}{lmm}(object, data = NULL, ...)

\method{model.frame}{lmm}(formula, ...)

\method{formula}{lmm}(x, fixed.only = TRUE, ...)

\method{terms}{lmm}(x, ...)

\method{anova}{lmm}(
  object,
  ...,
  type = 3,
  refit = TRUE,
  test = c("chisq", "parametric.bootstrap"),
  nsim = 199L,
  seed = NULL
)

\method{confint}{lmm}(object, parm = names(object$coefficients), level = 0.95, ...)
}
\arguments{
\item{x, object, formula}{An \code{lmm} object. The \code{formula} name follows the
argument name of the \code{model.frame()} generic.}

\item{...}{Additional fitted \code{lmm} models for likelihood-ratio comparison,
or arguments passed to the corresponding method.}

\item{adjusted}{Whether \code{vcov()} returns the Kenward-Roger-adjusted
covariance when available.}

\item{k}{Penalty per parameter used by \code{AIC()}.}

\item{type}{For \code{fitted()}, either \code{"conditional"} or \code{"marginal"}. For
\code{residuals()}, one of \code{"response"}, \code{"pearson"}, or \code{"marginal"}. For
\code{anova()}, only type \code{3} is implemented.}

\item{which}{Diagnostic plot to draw: standardized residuals against fitted
values, a normal quantile-quantile plot, or observed against fitted values.}

\item{newdata}{Optional data frame used for prediction.}

\item{re.form}{\code{NULL} includes every empirical random-effect term, \code{NA} or
\code{~ 0} excludes all random effects, and a random-effects formula or list of
formulas selects a subset of fitted terms.}

\item{allow.new.levels}{Whether conditional predictions may include new
grouping levels. Their random contribution is zero when allowed.}

\item{se.fit}{Whether \code{predict()} returns standard errors for the expected
response. These standard errors use the fixed-effect covariance and
condition on any empirical random effects included through \code{re.form}.}

\item{interval}{Prediction interval type. \code{"confidence"} describes the
expected response. \code{"prediction"} additionally includes the fitted
residual variance but not uncertainty in empirical random effects.}

\item{level}{Confidence level for fixed-effect and covariance intervals.}

\item{na.action}{Missing-value action retained for prediction-method
compatibility.}

\item{nsim}{Number of simulations for a parametric-bootstrap comparison.}

\item{seed}{Optional integer seed for a reproducible parametric bootstrap.}

\item{formula.}{Optional formula update accepted by \code{update()}.}

\item{evaluate}{Whether \code{update()} evaluates the updated call.}

\item{data}{Optional data frame used to construct a fixed-effects model
matrix.}

\item{fixed.only}{Whether \code{formula()} returns only the fixed formula. If
\code{FALSE}, it returns fixed, random, and repeated formulas in a list.}

\item{refit}{Whether REML models that differ in fixed effects are refitted
with ML before comparison.}

\item{test}{Reference test for likelihood-ratio model comparisons. The
default \code{"chisq"} uses the usual asymptotic chi-squared reference. The
\code{"parametric.bootstrap"} option simulates under each smaller model and is
appropriate when covariance parameters can lie on a boundary.}

\item{parm}{Parameters requested from \code{confint()}, supplied by name or fixed
effect position. Use \code{"beta_"} for every fixed effect and \code{"theta_"} for
every covariance parameter.}
}
\value{
The return value follows the corresponding base R generic.
}
\description{
Methods are provided for extracting likelihood quantities, fitted values,
residuals, model matrices and formulas, as well as for prediction, type III
testing and fixed-effect confidence intervals.
}
\details{
Conditional fitted values and predictions include empirical random effects.
Marginal values use fixed effects only. In prediction from new data, known
grouping levels use their fitted random effect. New levels are rejected by
default and receive a random contribution of zero only when explicitly
allowed.

\code{ranef()} always returns a named list with one tibble per random-effect
term, including for models with a single term. It returns an empty list for
marginal models. \code{sigma()} is defined as \code{sqrt(mean(diag(R)))}; it is a
descriptive residual scale when residual variances differ across
observations.
}
\references{
Self, S. G., and Liang, K.-Y. (1987). Asymptotic properties of maximum
likelihood estimators and likelihood ratio tests under nonstandard
conditions. \emph{Journal of the American Statistical Association}, 82(398),
605-610. \doi{10.1080/01621459.1987.10478472}

Davison, A. C., and Hinkley, D. V. (1997). \emph{Bootstrap Methods and Their
Application}. Cambridge University Press. \doi{10.1017/CBO9780511802843}
}
