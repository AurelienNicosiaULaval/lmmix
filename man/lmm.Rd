% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lmm.R
\name{lmm}
\alias{lmm}
\title{Fit a Gaussian linear mixed model}
\usage{
lmm(
  data,
  formula,
  random = NULL,
  repeated = NULL,
  structure = c("id", "cs", "ar1", "toep", "un"),
  method = c("REML", "ML"),
  ddf = c("satterthwaite", "residual", "kenward-roger"),
  control = lmm_control()
)
}
\arguments{
\item{data}{A data frame or tibble. Placing \code{data} first makes the function
pipe-friendly.}

\item{formula}{A two-sided fixed-effects formula.}

\item{random}{Optional one-sided random-effects formula such as
\code{~ 1 | subject} or \code{~ 1 + time | subject}.}

\item{repeated}{Optional one-sided repeated-measures formula such as
\code{~ time | subject}.}

\item{structure}{Residual covariance structure: \code{"id"}, \code{"cs"}, \code{"ar1"},
\code{"toep"}, or \code{"un"}.}

\item{method}{Estimation method: restricted maximum likelihood (\code{"REML"})
or maximum likelihood (\code{"ML"}).}

\item{ddf}{Denominator degrees-of-freedom method. \code{"satterthwaite"} and
\code{"residual"} are implemented. \code{"kenward-roger"} is reserved for future
implementation.}

\item{control}{An object returned by \code{lmm_control()}.}
}
\value{
An object of class \code{lmm}.
}
\description{
\code{lmm()} explicitly evaluates a profiled ML or REML criterion for
\verb{V = Z G Z' + R}. It can combine random effects with an independent,
compound-symmetric, AR(1), Toeplitz, or unstructured residual covariance.
}
\examples{
fit <- lmm(
  data = nlme::Orthodont,
  formula = distance ~ age + Sex,
  random = ~ 1 | Subject
)
summary(fit)
}
